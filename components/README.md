# Reusable Component Register

Components are retained assets that should be absorbed into destination products. They are not standalone products unless the canonical registry is changed through a Portfolio Governance Change.

The canonical component data lives in `../PORTFOLIO.yaml`.

Do not create a second manually maintained component register in this folder. Add narrative notes here only when they do not duplicate canonical destination/status fields.
